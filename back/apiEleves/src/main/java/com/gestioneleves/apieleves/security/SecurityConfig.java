package com.gestioneleves.apieleves.security;

/**
 * Placeholder conservé intentionnellement.
 *
 * L'application utilise {@link ApplicationSecurityConfig} pour toute la configuration Spring Security
 * (JWT, CORS, règles d'autorisations, gestion des erreurs). Ce fichier existe uniquement pour éviter
 * les confusions si des références historiques à "SecurityConfig" circulent encore dans la base.
 *
 * Ne pas ajouter de configuration ici — utiliser {@link ApplicationSecurityConfig} à la place.
 */
class SecurityConfig {
}
